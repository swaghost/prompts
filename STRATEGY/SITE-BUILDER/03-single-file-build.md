# 3. The Single File Build

Produces the actual thing you own.

Build this as ONE self contained HTML file. Inline all CSS in a style tag, no frameworks, no external libraries, no CDN links, no build step. Use system fonts. Semantic HTML with proper heading order. It must open correctly by double clicking it from my desktop with no internet connection. Output the complete file, not a snippet.
