# 5. The Speed and Search Pass

The unglamorous work that decides whether anyone finds it.

Add a title tag, meta description, Open Graph tags for link previews, favicon markup and JSON-LD structured data for a local business. Compress anything compressible and lazy load images below the fold. Then list every remaining thing that would slow the first paint, ordered by how much each one costs me.
